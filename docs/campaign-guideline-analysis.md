# 캠페인 가이드라인 자동 분석 시스템

## 개요

체험단 일정 등록 시 AI를 활용하여 캠페인 가이드라인을 자동으로 분석하고, 추출된 데이터를 기반으로 일정을 자동으로 채워주는 시스템입니다.

## 기능

### 1. AI 가이드라인 분석
- **입력**: 캠페인 가이드라인 텍스트 (이미지, 텍스트, 링크 등)
- **출력**: 구조화된 캠페인 정보

### 2. 자동 데이터 추출
- 📋 **기본 정보**: 캠페인 제목, 포인트, 모집 기간, 리뷰 등록 기간
- 📅 **주요 일정**: 모든 중요한 마감일 목록
- 💰 **보상 정보**: 지급 포인트, 제공 내역, 배송 방법
- 📝 **컨텐츠 요구사항**: 제목/본문 키워드, 이미지 수, 동영상, 링크, 최소 글자수
- 🔔 **필수 공지 문구**: 공정위 대가성 등 법적 문구
- 🎯 **미션 세부사항**: 각 플랫폼별 상세 미션 내용
- ⚠️ **주의사항**: 해서는 안 될 일들, 페널티 관련 사항

### 3. 사용자 검증 및 선택
- AI 분석 결과를 사용자가 검토
- 필요한 데이터만 선택적으로 적용
- 분석 결과 JSON 복사 가능

### 4. 일정 자동 채우기
- 분석된 정보를 체험단 일정 폼에 자동으로 입력
- 추가 마감일 자동 생성
- 메모에 캠페인 정보 자동 기록

## 아키텍처

### API 엔드포인트

```
POST /api/ai/parse-guideline
```

**요청:**
```json
{
  "guideline": "가이드라인 텍스트..."
}
```

**응답:**
```json
{
  "success": true,
  "data": {
    "title": "캠페인 제목",
    "points": 30000,
    "recruitPeriod": { "start": "2026-02-09", "end": "2026-02-11" },
    "reviewerAnnouncement": "2026-02-12",
    "reviewRegistrationPeriod": { "start": "2026-02-13", "end": "2026-02-26" },
    "deadlines": [...],
    "rewardInfo": {...},
    "contentRequirements": {...},
    "requiredNotices": [...],
    "missions": [...],
    "importantNotes": [...],
    "warnings": [...]
  }
}
```

### UI 컴포넌트

#### 1. `GuidelineAnalysisModal`
- 가이드라인 텍스트 입력 폼
- AI 분석 진행 중 표시
- 분석 결과 프리뷰
- JSON 복사 기능

#### 2. `GuidelineInfoModal`
- 탭 기반 상세 정보 표시
  - 보상정보
  - 컨텐츠 요구사항
  - 미션 세부사항
  - 필수 공지 문구
  - 중요사항
  - 주의사항

#### 3. 스케줄 모달 통합
- "AI로 가이드라인 분석하기" 버튼
- "분석된 가이드라인 정보 보기" 버튼
- 분석된 데이터 자동 입력

## 사용 흐름

### 1단계: 가이드라인 입력
```
체험단 모달 열기
  → "AI로 가이드라인 분석하기" 버튼 클릭
    → 가이드라인 텍스트 붙여넣기
      → "분석하기" 버튼 클릭
```

### 2단계: 결과 검증
```
분석 결과 프리뷰 확인
  → 각 섹션별 정보 검토
    → 필요시 JSON 복사
      → "이 데이터로 일정 생성" 클릭
```

### 3단계: 자동 채우기
```
분석된 정보가 일정 폼에 자동 입력
  → 추가 마감일 자동 생성
    → 메모에 캠페인 정보 기록
      → 사용자가 나머지 정보 입력 후 저장
```

## 데이터 타입

### `CampaignGuidelineAnalysis`

```typescript
interface CampaignGuidelineAnalysis {
  title: string;
  points: number;
  recruitPeriod: {
    start: string;  // YYYY-MM-DD
    end: string;    // YYYY-MM-DD
  };
  reviewerAnnouncement: string;  // YYYY-MM-DD
  reviewRegistrationPeriod: {
    start: string;
    end: string;
  };
  deadlines: CampaignGuidelineDeadline[];
  rewardInfo: {
    description: string;
    points: number;
    deliveryMethod: string;
    productInfo?: string;
  };
  contentRequirements: {
    titleKeywords: CampaignGuidelineKeyword[];
    bodyKeywords: CampaignGuidelineKeyword[];
    requirements: CampaignGuidelineRequirement[];
  };
  requiredNotices: string[];
  missions: CampaignGuidelineMission[];
  importantNotes: string[];
  warnings: string[];
}
```

## 기술 스택

- **Backend**: Next.js API Routes
- **AI Model**: Claude 3.5 Sonnet (Anthropic)
- **UI Components**: React + shadcn/ui
- **Data Management**: TypeScript

## 환경 변수

```env
ANTHROPIC_API_KEY=your_anthropic_api_key
```

## 주요 파일

| 파일 | 설명 |
|------|------|
| `/app/api/ai/parse-guideline/route.ts` | 가이드라인 분석 API |
| `/components/guideline-analysis-modal.tsx` | 분석 폼 및 결과 모달 |
| `/components/guideline-info-modal.tsx` | 상세 정보 모달 |
| `/components/schedule-modal.tsx` | 스케줄 모달 (통합) |
| `/types/index.ts` | TypeScript 타입 정의 |

## 사용 예시

### 가이드라인 분석 호출

```typescript
const response = await fetch('/api/ai/parse-guideline', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    guideline: `[회수형/30,000P] 올메라 바디핏롤러 추천해요♥
[리뷰 후 제품 회수] 올메라 바디핏롤러 + 리뷰플레이스30,000p
...`
  })
});

const result = await response.json();
console.log(result.data);  // CampaignGuidelineAnalysis
```

## 주의사항

1. **정확도**: AI 분석 결과는 가이드라인의 구조와 내용에 따라 달라질 수 있습니다
2. **검증**: 자동으로 채워진 데이터는 항상 사용자가 검증해야 합니다
3. **API Key**: Anthropic API Key가 필수적으로 필요합니다
4. **처리 시간**: 긴 가이드라인의 경우 분석에 시간이 걸릴 수 있습니다

## 향후 개선 사항

- [ ] 이미지 기반 OCR 가이드라인 인식
- [ ] 플랫폼별 가이드라인 템플릿 학습
- [ ] 사용자 피드백 기반 분석 정확도 개선
- [ ] 분석 결과 캐싱
- [ ] 배치 분석 지원
- [ ] 분석 결과 수정 기능

## 라이센스

MIT
